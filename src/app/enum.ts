export enum Action { create, read, update, delete }
export enum Status { sucess, warning, fail }
export enum Element { graph, branch, node, edge, attribut, user }
export enum EDGE_TYPE{ hierarchy, custom }